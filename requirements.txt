# Kp Index Space Weather Monitor - Requirements
# Core data processing and web requests
pandas>=1.5.0,<3.0.0
requests>=2.28.0,<3.0.0

# Email functionality (built-in smtplib is used, no additional requirements)
# Logging functionality (built-in logging is used, no additional requirements)

# Optional: For enhanced CSV processing (pandas already includes this)
# python-dateutil>=2.8.0  # Included with pandas

# Development and testing (optional)
# pytest>=7.0.0
# flake8>=5.0.0 